                var regExp = /^[0-9]{10}$/;

                if (!regExp.test(bNo)) {
                    // 유효하지 않은 사업자 등록번호일 경우 메시지를 alert로 표시하고 입력 필드에 포커스 주기
                    alert("유효하지 않은 사업자 등록번호입니다.");
                    $("#b_no").focus();
                    return;
                }
